package  
{
	/**
	 * ...
	 * @author mhtsu
	 * 使う用途未定　←それな
	 */
	public class stageSet 
	{
		public var enemys:Vector.<enemySet> = new Vector.<enemySet> //敵情報
		public var str:String = new String;
		public function stageSet(ENEMYS:Vector.<enemySet>) 
		{
			enemys = ENEMYS;
		}
		
	}

}